// const france = "france";
// const paris = "paris";
// const marsielle = france;

// const vacationSpots = (spot1, spot2) => {
//   if (spot1.length > spot2.length) {
//     return spot1;
//   }
//   return spot2;
// };

//practicing code
let france = "france";
function vacationSpots(val1, val2) {
  if (val1.length > val2.length) {
    return val1;
  }
  return val2;
}
